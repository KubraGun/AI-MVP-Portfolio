## contents of .db files isn't visible

1. what is its type?
- Control
```bash
type <file_name>.db | more

```



[NOTE] When running Python code while the SQLite Viewer is open in VS Code, the SQLite file becomes locked or corrupted.